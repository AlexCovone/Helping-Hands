<%- include('partials/header') -%>
<div class="container">
  <div class="row my-5">
      <div class="col-6">
          <div>
              <p><strong>User Name</strong>: <%= user.userName %></p>
              <p><strong>Email</strong>: <%= user.email %></p>
              <a href="/logout" class="col-3 btn btn-primary">Logout</a>
          </div>
      </div>
      <div class="col-6">
        <ul class="row list-unstyled">
        <% for(var i=0; i<reservation.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/reservation/<%= reservation[i].event._id%>">
              <h2><%= reservation[i].event.eventName %></h2>
              <img class="img-fluid" src="<%= reservation[i].event.image%>">
            </a>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/events">Return to Feed</a>
        </div>  
      </div>  
    </div>
  </div>
</div>

<script>
  let profileLink = document.querySelector('#profilelink');
  profileLink.classList.add('active'); 
</script>

<%- include('partials/footer') -%>